import React from "react";
import { createRoot } from "react-dom/client";
import TeacherHub from "./TeacherHub.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TeacherHub />
  </React.StrictMode>
);
